Look and Feel
import javax.swing.UIManager;
        try {
            UIManager.setLookAndFeel("javax.swing.plaf.nimbus.NimbusLookAndFeel");
        } catch (Exception e) {
            e.printStackTrace();
        }
setBackground(), setForeground(), setOpaque() i
Layout Managers

FX 3D
compose multiplatform - z adoridem

//        for(int i = 0; i < 3; i++) {
//           for(inr j = 0; j < 3; j++) {
//
//           }
//        }

board.getTileBoard()[k][l].add(this.addAncestorListener(););

//        end of loop
       for(int i = 0; i < 3; i++) {
           for (int j = 0; j < 3; j++) {
//               System.out.println(this.getMainBoard()[i][j]);
           }
       }